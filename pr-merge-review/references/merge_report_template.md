# PR 合并风险审查报告

> 本模板由 `pr-merge-review` skill 生成。生成报告时替换 `<...>` 占位符为实际内容。
> 报告面向 PR 提交者，语气专业、尊重、协作，可直接复制到 PR 评论中。

---

## 1. 报告概要

| 项 | 内容 |
|---|---|
| 目标分支 (target) | `<main>` |
| 开发分支 (dev / PR) | `<feature/xxx>` (#PR_NUMBER) |
| 实际分析 refs | `<target-ref>` ← `<dev-ref>` |
| merge base | `<commit-sha>` |
| 是否执行 fetch | 是 / 否 |
| 本地与远端一致性 | 一致 / ahead / behind / diverged |
| 当前工作区是否干净 | 干净 / 有未提交改动（列出文件） |
| PR 意图摘要 | `<一段话总结 PR 目的>` |

**合并决策建议**: `<Approve | Approve with fixes | Request Changes | Block>`

**合并风险等级**: `<高 | 中 | 低>`

- 🔴 阻塞项：`<N>` 项
- 🟠 严重项：`<N>` 项
- 🟡 重要项：`<N>` 项
- 🟢 小问题 / 💡 建议 / 📚 学习：`<N>` 项

**target 功能保护**:

- 🔴 破坏 target 现有功能：`<N>` 项
- 🟠 高风险破坏：`<N>` 项
- 🟡 间接影响：`<N>` 项
- 🟢 无影响：`<N>` 项

> ⚠️ 若存在任何 🔴 破坏 target 功能的项，此处高亮标注："**合并将破坏 target 分支的 N 项现有功能，详见第 6 章**"

**总体评价**:

> `<一段话总体评价，先肯定优点，再点明对 target 现有功能的破坏风险（核心），再说明 dev 功能合入风险，最后给出合并建议>`

---

## 2. 变更概览

### 2.1 功能摘要

`<一段话描述本次变更带来的功能/行为变化>`

### 2.2 commit 列表

```
<commit-hash> <commit-message>
<commit-hash> <commit-message>
...
```

### 2.3 改动文件清单

| 状态 | 文件路径 | 说明 |
|---|---|---|
| A | `<path>` | 新增 |
| M | `<path>` | 修改 |
| D | `<path>` | 删除 |
| R | `<old>` → `<new>` | 重命名 |
| 🔒 | `<lockfile>` | lockfile |
| 📦 | `<migration/schema/generated>` | 特殊文件 |

### 2.4 模块影响范围

`<前端 / 后端 / 数据库 / 配置 / CI 等>`

### 2.5 PR 大小评估

- 改动行数：`<+XXX / -XXX>`
- 改动文件数：`<N>`
- 评估：`<适中 / 过大建议拆分 / 较小>`

---

## 3. 优点

> 先肯定本次变更做得好的地方，建立信任。

1. 🎉 `<具体优点 1，引用文件:行号>`
2. 🎉 `<具体优点 2>`
3. 🎉 `<具体优点 3>`

---

## 4. 代码审查结果

### 4.1 public API changes

| 类型 | 位置 | 变更说明 | 破坏性 | 引用搜索结论 |
|---|---|---|---|---|
| 函数签名 | `<file:line>` | `<说明>` | 是/否 | 无引用 / 有引用需同步 / 不确定 |

### 4.2 dependencies

`<该分支依赖或影响的模块>`

### 4.3 special files

| 类型 | 文件 | 风险说明 |
|---|---|---|
| lockfile | `<path>` | `<说明>` |
| migration | `<path>` | `<说明>` |
| schema | `<path>` | `<说明>` |
| generated | `<path>` | `<说明>` |

### 4.4 test coverage

- 新增测试：`<是/否，列出文件>`
- 改动点是否被现有测试覆盖：`<是/否/部分>`
- 缺失覆盖的关键路径：`<说明>`

### 4.5 concerns

`<潜在 bug、边界情况、风格不一致、兼容性风险>`

### 4.6 审查覆盖度统计

```text
改动文件审查覆盖率：已处理 <N> / 总数 <N>
```

---

## 5. dev 功能合入完整性矩阵

| 功能项 | 入口点 | 涉及文件 | 依赖项 | 关联冲突/风险 | 解决建议 | 验证方式 | 合并完整性结论 |
|---|---|---|---|---|---|---|---|
| F1 | `<UI/API/CLI>` | `<files>` | `<deps>` | `<R1, C1a>` | `<建议>` | `<验证>` | 可完整合入 |
| F2 | ... | ... | ... | ... | ... | ... | 需调整后合入 |
| F3 | ... | ... | ... | ... | ... | ... | 不确定 |

### 5.1 未映射改动

| 文件 | 原因 | 是否影响合并完整性 |
|---|---|---|
| `<path>` | 纯格式化 / 文档补充 / 生成文件 / 无法判断业务意图 | 是/否 |

---

## 6. target 功能保护影响矩阵（核心章节）

> **合并后是否会破坏 target 分支现有功能/契约？这是合并审查的核心问题。**
> 若存在任何 🔴 破坏项，必须在报告概要中高亮标注，并列为合并前必须处理的阻塞项。

| target 功能 | 入口点 | 涉及 target 文件 | 影响等级 | 影响说明 | 关联 dev 改动 | 保护建议 | 验证方式 |
|---|---|---|---|---|---|---|---|
| T1 | `<UI/API/CLI>` | `<files>` | 🔴 破坏 | `<具体如何被破坏>` | `<F2, R1>` | `<保护/修复建议>` | `<验证 target 功能仍正常>` |
| T2 | ... | ... | 🟠 高风险 | `<...>` | `<F1>` | `<...>` | `<...>` |
| T3 | ... | ... | 🟡 间接 | `<...>` | `<F3>` | `<...>` | `<...>` |
| T4 | ... | ... | 🟢 无影响 | — | — | — | — |

### 6.1 target 功能破坏模式分类

按破坏类型汇总（便于 PR 提交者快速定位）：

| 破坏类型 | 受影响 target 功能 | 关联风险 |
|---|---|---|
| 公共接口破坏性变更 | `<T1, T3>` | `<R1, R4>` |
| 运行时行为改变 | `<T2>` | `<R2>` |
| 数据/schema 破坏 | `<T5>` | `<R6>` |
| 配置/环境破坏 | — | — |
| 测试/CI 破坏 | — | — |
| 隐式依赖破坏 | `<T4>` | `<R3>` |

---


## 7. 冲突清单

### 6.1 文本冲突

| 冲突编号 | 文件 | 冲突块 | 双方意图 | 关联功能项 | 特殊类型 |
|---|---|---|---|---|---|
| C1a | `<file:line>` | `<块说明>` | dev: ... / target: ... | F1 | rename/delete 等 |

### 6.2 语义冲突

| 冲突编号 | 类型 | 位置 | 说明 | 影响方向 | 关联功能项 |
|---|---|---|---|---|---|
| S1 | 函数签名变化 | `<file:line>` | `<说明>` | dev→target | F2 |

### 6.3 结构冲突

| 冲突编号 | 类型 | 文件 | 说明 | 关联功能项 |
|---|---|---|---|---|
| T1 | migration 顺序 | `<file>` | `<说明>` | F3 |

### 6.4 特殊文件冲突

| 冲突编号 | 类型 | 文件 | 说明 | 建议处理方式 |
|---|---|---|---|---|
| L1 | lockfile | `<file>` | `<说明>` | 重新生成 |

---

## 8. 风险评估清单

> 按严重程度分组，**阻塞项优先展示，target 功能破坏项次优先**。每条包含：位置、问题、为什么重要、修复建议、置信度。

### 8.1 🔴 阻塞项（合并前必须修复）

#### R1: `<问题描述>`

- **严重程度**: 🔴 blocking
- **类别**: `<correctness / security / conflict-text / breaking-change / target-regression / migration ...>`
- **影响方向**: `dev→target（破坏 target）` / `target→dev（影响 dev）` / `双向` / `无方向`
- **位置**: `<file:line>`
- **关联 dev 功能**: `<F_id>`
- **关联 target 功能**: `<T_id>`（若破坏了 target 现有功能，必须标注）
- **问题**: `<具体说明>`
- **为什么重要**: `<风险/影响>`
- **修复建议**: `<具体建议>`
- **置信度**: `✅ AI 已给出确定方案` / `⚠️ AI 不确定，需人工确认`
- **需人工确认**: 是/否，原因

---

### 8.2 🟠 严重项

#### R2: `<问题描述>`

- **严重程度**: 🟠 critical
- **类别**: `<...>`
- **影响方向**: `<...>`
- **位置**: `<file:line>`
- **关联 dev 功能**: `<F_id>`
- **关联 target 功能**: `<T_id>`
- **问题**: `<具体说明>`
- **为什么重要**: `<...>`
- **修复建议**: `<...>`
- **置信度**: `<...>`

---

### 8.3 🟡 重要项

#### R3: `<问题描述>`

- **严重程度**: 🟡 important
- **类别**: `<...>`
- **影响方向**: `<...>`
- **位置**: `<file:line>`
- **关联 dev 功能**: `<F_id>`
- **关联 target 功能**: `<T_id>`
- **问题**: `<...>`
- **修复建议**: `<...>`
- **置信度**: `<...>`

---

### 8.4 🟢 小问题 / 💡 建议 / 📚 学习 / 🎉 亮点

- 🟢 `[nit]` `<file:line>`: `<说明>`
- 💡 `[suggestion]` `<file:line>`: `<说明>`
- 📚 `[learning]` `<file:line>`: `<说明>`
- 🎉 `[praise]` `<file:line>`: `<说明>`

---

## 9. 冲突解决方案

### C1a: `<冲突说明>`

- **关联**: F1 / T2, R1（同时标注 dev 功能和 target 功能）
- **文件**: `<file:line>`
- **冲突类型**: 文本 / 语义 / 结构 / 特殊文件
- **严重程度**: 🔴 / 🟠 / 🟡
- **dev 方意图**: `<...>`
- **target 方意图**: `<...>`
- **建议解决方案**: `<...>`
- **合并后代码片段**（如适用）:

  ```text
  ✅ AI 已给出确定方案
  <code>
  ```

  或

  ```text
  ⚠️ AI 不确定，需人工确认，原因：...
  ```

- **对 dev 功能完整性的影响**: 完整保留 / 需补充修改 / 不确定 / 可能改变行为
- **对 target 现有功能的影响**: 无影响 / 行为改变 / 契约破坏 / 数据破坏
- **验证方式**: `<验证 dev 功能 + 验证 target 功能>`

---

## 10. 验证与测试方案

### 10.1 应运行的测试命令

```bash
<npm test>
<npm run lint>
<npm run build>
<pytest tests/...>
```

### 10.2 针对每个 dev 功能项的验收检查

| 功能项 | 验证方式 | 期望结果 |
|---|---|---|
| F1 | `<具体测试用例或手动步骤>` | `<期望结果>` |

### 10.3 针对每个 target 功能项的回归验证（核心）

> 合并后必须确认 target 现有功能仍然正常，尤其是被标注为 🔴/🟠 影响的项。

| target 功能 | 影响等级 | 回归验证方式 | 期望结果 |
|---|---|---|---|
| T1 | 🔴 破坏 | `<具体测试用例或手动步骤>` | target 功能行为不变 |
| T2 | 🟠 高风险 | `<...>` | `<...>` |

### 10.4 针对每个冲突解决点的测试建议

| 冲突/风险 | 测试建议 |
|---|---|
| C1a / R1 | `<验证 dev 功能 + 验证 target 功能>` |

### 10.5 接口变更回归检查

| public API change | 受影响调用方（含 target 侧） | 回归测试 |
|---|---|---|
| `<说明>` | `<调用方清单>` | `<测试命令或用例>` |

### 10.6 CI 检查建议

- `<应配置或检查的 CI 项>`

### 10.7 人工复核清单

| 需确认项 | 原因 | 应由谁确认 | 关注点 |
|---|---|---|---|
| `<⚠️ AI 不确定，需人工确认的项>` | `<为什么需要人工确认>` | PR 提交者 / 维护者 / 业务负责人 | `<关注什么>` |

### 10.8 灰度和回滚策略（如适用）

- 是否建议分批合并：`<是/否>`
- 回滚步骤：`<...>`
- feature flag 使用建议：`<...>`

---

## 11. 覆盖度统计

```text
改动文件审查覆盖率：已处理 <N> / 总数 <N>
dev 功能项覆盖率：已归类 <N> / 总功能项 <N>
target 功能项保护评估覆盖率：已评估 <N> / 受影响总数 <N>
未映射改动数量：<N>
文本冲突覆盖率：已处理 <N> / 总数 <N>
语义冲突覆盖率：已处理 <N> / 总数 <N>
冲突到功能项映射覆盖率：已映射 <N> / 总功能项 <N>
public API 引用搜索覆盖率：已处理 <N> / 总数 <N>
```

---

## 12. 合并决策总结

### 12.1 功能完整性结论

- dev 分支功能是否可完整合入 target: `<是 / 否 / 部分可>`
- **可完整合入的功能**: `<F1, F2>`
- **需调整后合入的功能**: `<F3>`
- **不确定或需人工确认的功能**: `<F4>`

### 12.2 target 功能保护结论（核心判断）

- **合并后是否会破坏 target 现有功能**: `<否 / 是，破坏 N 项>`
- **被破坏的 target 功能清单**: `<T1, T3>`
- **破坏类型**: `<公共接口破坏性变更 / 运行时行为改变 / 数据schema破坏 / ...>`
- **是否可修复后合入**: `<是 / 否，原因>`

### 12.3 合并前必须处理的阻塞项

- [ ] R1: `<问题描述>`
- [ ] R2: `<问题描述>`

### 12.4 最终建议

**合并决策**: `<Approve | Approve with fixes | Request Changes | Block>`

**理由**:

> `<1-2 句技术评估说明，例如：核心实现扎实但存在 2 项阻塞风险（migration 顺序错误、公共接口破坏性变更破坏 target T1、T3），建议修复 R1、R2 后重新审查。>`

### 12.5 PR 提交者下一步建议

1. `<具体动作 1，例如修复 R1 的 migration 顺序>`
2. `<具体动作 2，例如恢复被破坏的 target 公共接口>`
3. `<回复本报告中对 ⚠️ AI 不确定，需人工确认 项的疑问>`
4. `<修复完成后请求重新审查>`

---

## 13. 限制与假设

- 是否未 fetch: `<是/否>`
- 是否未执行实际合并探测: `<是/否>`
- 是否存在脏工作区: `<是/否，列出文件>`
- 是否有无法确定的业务语义: `<是/否，说明>`
- 是否缺乏 PR 描述/业务上下文: `<是/否，影响说明>`
- subagent 覆盖范围: `<列出哪些任务由 subagent 完成>`

---

> 本报告由 `pr-merge-review` skill 自动生成，仅供合并前风险评估参考。
> 阻塞项和严重项必须在合并前处理；标注 ⚠️ AI 不确定，需人工确认 的项需人工复核。
